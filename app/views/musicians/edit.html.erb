<%= error_messages_for(@musician) %>
<%= form_for(@musician, url: {controller: @instrument.to_s.pluralize, action: 'update', method: :PUT}) do |u| %>
  <%= render(partial: 'form', locals: {u: u}) %>
<% end %>
