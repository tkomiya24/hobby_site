<%= error_messages_for(@musician) %>
<%= form_for(@musician, as: :instrument, url: musicians_path, html: {role: 'form', method: :post}) do |u| %>
  <%= render(partial: 'form', locals: {u: u}) %>
  <%= hidden_field(:instrument, :instrument, value: @instrument) %>
<% end %>
